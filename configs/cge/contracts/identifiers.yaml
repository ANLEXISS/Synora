schema_version: 1
namespace: synora.cge
identifiers:
  - {id: event_id, semantic: historical source event identity, owner: historical_core, generator: source_or_core, scope: historical_event, uniqueness: event_scope, wire_type: string, protection: cgeid_v1_observation_at_cge_boundary, domain: observation, nullable: false, deduplication: historical_event_policy, correlation: historical_and_cge_reference, ordering: source_order, persistence: [synora.store.core-state], restart_stability: legacy_store, legacy_behavior: accepted_raw_only_in_core_history}
  - {id: observation_id, semantic: durable CGE observation identity, owner: cge_adapter, generator: ProtectRaw(event_id), scope: cge_observation, uniqueness: observation_scope, wire_type: string, protection: cgeid_v1_observation, domain: observation, nullable: false, deduplication: observation_id, correlation: all_cge_subsystems, ordering: observation_order, persistence: [synora.store.cge-journal, synora.store.workflow-wal], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: entity_id, semantic: known subject identity, owner: cge_adapter, generator: ProtectRaw(identity), scope: cge_subject, uniqueness: subject_domain, wire_type: string, protection: cgeid_v1_entity, domain: entity, nullable: true, deduplication: subject_identity, correlation: subject_observations, ordering: none, persistence: [synora.store.cge-journal, synora.store.workflow-wal, synora.store.calibration-ledger], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: candidate_entity_id, semantic: uncertain subject candidate identity, owner: cge_adapter, generator: ProtectRaw(identity), scope: cge_subject_candidate, uniqueness: subject_domain, wire_type: string, protection: cgeid_v1_entity, domain: entity, nullable: true, deduplication: candidate_identity, correlation: uncertain_observations, ordering: none, persistence: [synora.store.workflow-wal], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: device_id, semantic: source device identity, owner: cge_adapter, generator: ProtectRaw(device_id), scope: cge_device, uniqueness: device_domain, wire_type: string, protection: cgeid_v1_device, domain: device, nullable: true, deduplication: device_identity, correlation: device_observations, ordering: source_order, persistence: [synora.store.cge-journal, synora.store.workflow-wal], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: camera_id, semantic: camera identity alias, owner: cge_adapter, generator: ProtectRaw(device_id), scope: cge_device, uniqueness: device_domain, wire_type: string, protection: cgeid_v1_device, domain: device, nullable: true, deduplication: device_identity, correlation: device_observations, ordering: source_order, persistence: [synora.store.cge-journal], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: node_id, semantic: topology node identity, owner: topology, generator: configured_topology, scope: topology, uniqueness: topology_config, wire_type: string, protection: topology_preserved, domain: topology, nullable: false, deduplication: topology_node, correlation: spatial_context, ordering: topology_order, persistence: [], restart_stability: configured, legacy_behavior: accepted}
  - {id: zone_id, semantic: topology zone identity, owner: topology, generator: configured_topology, scope: topology, uniqueness: topology_config, wire_type: string, protection: topology_preserved, domain: topology, nullable: true, deduplication: topology_zone, correlation: spatial_context, ordering: topology_order, persistence: [], restart_stability: configured, legacy_behavior: accepted}
  - {id: clip_id, semantic: media clip reference, owner: cge_adapter, generator: source_clip, scope: cge_observation, uniqueness: clip_scope, wire_type: string, protection: cgeid_v1_clip, domain: clip, nullable: true, deduplication: clip_reference, correlation: observation_media, ordering: clip_order, persistence: [synora.store.cge-journal], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: track_id, semantic: media track reference, owner: cge_adapter, generator: source_track, scope: cge_observation, uniqueness: track_scope, wire_type: string, protection: cgeid_v1_track, domain: track, nullable: true, deduplication: track_reference, correlation: observation_media, ordering: track_order, persistence: [synora.store.cge-journal], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: activation_id, semantic: activation reference, owner: cge_adapter, generator: source_activation, scope: cge_observation, uniqueness: activation_scope, wire_type: string, protection: cgeid_v1_activation, domain: activation, nullable: true, deduplication: activation_reference, correlation: observation_activation, ordering: activation_order, persistence: [synora.store.workflow-wal], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: sequence_key, semantic: source sequence correlation key, owner: cge_adapter, generator: source_sequence, scope: cge_observation, uniqueness: sequence_scope, wire_type: string, protection: cgeid_v1_sequence, domain: sequence, nullable: true, deduplication: sequence_policy, correlation: temporal_sequence, ordering: sequence_order, persistence: [synora.store.cge-journal, synora.store.workflow-wal], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: episode_id, semantic: cognitive episode identity, owner: cge_episodes, generator: episode_registry, scope: cge_episode, uniqueness: episode_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: episode_identity, correlation: episode_observations, ordering: episode_revision, persistence: [synora.store.workflow-wal, synora.store.workflow-checkpoint], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: chain_id, semantic: cognitive chain identity, owner: cge_chains, generator: chain_registry, scope: cge_chain, uniqueness: chain_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: chain_identity, correlation: chain_observations, ordering: chain_revision, persistence: [synora.store.cge-journal, synora.store.cge-generations], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: hypothesis_set_id, semantic: hypothesis set identity, owner: cge_hypotheses, generator: hypothesis_registry, scope: cge_hypothesis_set, uniqueness: set_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: set_identity, correlation: hypothesis_lineage, ordering: revision, persistence: [synora.store.workflow-wal], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: fact_id, semantic: derived fact identity, owner: cge_facts, generator: fact_registry, scope: cge_fact, uniqueness: fact_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: fact_identity, correlation: fact_provenance, ordering: revision, persistence: [synora.store.workflow-wal], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: evaluation_id, semantic: evidence evaluation identity, owner: cge_evidence, generator: evaluation_registry, scope: cge_evaluation, uniqueness: evaluation_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: evaluation_identity, correlation: evidence_lineage, ordering: revision, persistence: [synora.store.workflow-wal], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: situation_id, semantic: cognitive situation identity, owner: cge_situation, generator: situation_registry, scope: cge_situation, uniqueness: situation_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: situation_identity, correlation: situation_lineage, ordering: revision, persistence: [synora.store.workflow-wal], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: recommendation_set_id, semantic: recommendation set identity, owner: cge_recommendation, generator: recommendation_registry, scope: cge_recommendation_set, uniqueness: set_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: set_identity, correlation: recommendation_lineage, ordering: revision, persistence: [synora.store.workflow-wal], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: comparison_id, semantic: historical comparison identity, owner: cge_comparison, generator: comparison_registry, scope: cge_comparison, uniqueness: comparison_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: comparison_identity, correlation: comparison_lineage, ordering: revision, persistence: [synora.store.calibration-ledger], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: transaction_id, semantic: workflow transaction identity, owner: cge_workflow, generator: transaction_registry, scope: workflow, uniqueness: transaction_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: transaction_id, correlation: workflow_commit, ordering: workflow_sequence, persistence: [synora.store.workflow-wal], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: journal_id, semantic: CGE journal identity, owner: cge_chains_journal, generator: journal_constructor, scope: journal, uniqueness: journal_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: journal_id, correlation: journal_records, ordering: journal_sequence, persistence: [synora.store.cge-journal], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: ledger_record_id, semantic: calibration ledger record identity, owner: cge_calibration_ledger, generator: ledger_append, scope: ledger, uniqueness: ledger_scope, wire_type: string, protection: derived_fingerprint_only, domain: derived, nullable: false, deduplication: record_fingerprint, correlation: ledger_record, ordering: ledger_sequence, persistence: [synora.store.calibration-ledger], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: revision, semantic: logical aggregate revision, owner: cge_component, generator: component_revision, scope: component_state, uniqueness: monotonic_revision, wire_type: uint64, protection: none, domain: ordering, nullable: false, deduplication: revision, correlation: state_version, ordering: monotonic, persistence: [synora.store.workflow-wal], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: sequence, semantic: store-local monotonic sequence, owner: cge_store, generator: store_append, scope: store, uniqueness: store_sequence, wire_type: uint64, protection: none, domain: ordering, nullable: false, deduplication: sequence, correlation: record_order, ordering: monotonic, persistence: [synora.store.cge-journal, synora.store.workflow-wal, synora.store.calibration-ledger], restart_stability: recovered, legacy_behavior: legacy_replay_only}
  - {id: digest, semantic: deterministic state digest, owner: cge_component, generator: sha256, scope: state, uniqueness: content_fingerprint, wire_type: string, protection: hash_only, domain: derived, nullable: false, deduplication: digest, correlation: recovery_integrity, ordering: none, persistence: [synora.store.workflow-wal, synora.store.calibration-ledger], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
  - {id: fingerprint, semantic: deterministic schema or content fingerprint, owner: cge_component, generator: sha256, scope: contract_or_state, uniqueness: content_fingerprint, wire_type: string, protection: hash_only, domain: derived, nullable: false, deduplication: fingerprint, correlation: provenance_and_recovery, ordering: none, persistence: [synora.store.cge-journal, synora.store.workflow-wal, synora.store.calibration-ledger], restart_stability: deterministic, legacy_behavior: legacy_replay_only}
